#!/usr/bin/env ruby

require_relative '../lib/turn'
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)
turn()
if valid_move?(board, input_to_index(gets.chomp.strip))
  
else
  turn()
end
